language: rust
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly
cache: cargo
script:
  - cargo build --verbose --all --features std,serialize,deserialize
  - cargo test --verbose --all --features std,serialize,deserialize
